# Card Apply — card-service Spec

> **Service:** card-service
> **Contract Module:** card-contract
> **Caller:** bff → [mobile-bff/card-apply.md](../mobile-bff/card-apply.md)
> **Last Updated:** 2026-03

---

## 1. Overview

Receives the card application request from BFF via Feign.
Validates that the user does not already hold an active card of the same type.
Generates a mock PAN (BIN prefix + `SecureRandom` digits + Luhn validation), derives `pan_masked` and `pan_hash`, generates a one-time CVV, computes the expiry date (application date + 5 years), encrypts all sensitive fields with AES-256-GCM, and persists the card record with `status = ACTIVE`.
Returns the card details including the **one-time CVV** to BFF.

> **CVV:** Generated by `SecureRandom`. Never stored in DB, Redis, or logs. Exists only in JVM heap during this request and is included once in the response.

> **pan_hash:** HMAC-SHA256 of the full PAN. Stored with a unique index. Required by Phase 7 (Issuer Authorization) for PAN-to-member lookup without storing plain PAN.

---

## 2. API Definition — card-service

### Endpoint

| Field | Value |
|-------|-------|
| Method | POST |
| context-path | `/card` |
| Controller mapping | `/apply` |
| Effective received path | `/card/apply` |
| Caller | BFF (`CardFeignClient`) |

### Request (`card.contract.dto.CardApplyRq`)

| Field | Type | Required | Validation | Description |
|-------|------|----------|------------|-------------|
| cardType | String | ✅ | @NotBlank | CLASSIC / OVERSEAS / PREMIUM / INFINITE |
| cardNetwork | String | ✅ | @NotBlank | VISA / MASTERCARD / JCB |
| cardholderNameEn | String | ✅ | @NotBlank | Cardholder English name |
| cardholderNameZh | String | ✅ | @NotBlank | Cardholder Chinese name |

### Response (`card.contract.dto.CardApplyRs`)

| Field | Type | Description | Notes |
|-------|------|-------------|-------|
| cardId | String | Snowflake ID (serialized as String) | |
| maskedPan | String | `****-****-****-1234` | |
| cardType | String | Card type | |
| cardNetwork | String | Card network | |
| expiryDate | String | `MM/YY` format (decrypted for response) | Stored encrypted as `MMYY` |
| cardholderNameEn | String | Cardholder English name | Decrypted for response; stored encrypted |
| cvv | String | 3-digit CVV | One-time; never stored |
| status | String | `ACTIVE` | |
| activatedAt | String | ISO-8601 timestamp | |

---

## 3. Service UseCase Flow

**UseCase:** `CardApplyUseCase.apply(CardApplyRq)` (card-service)
**UseCase Impl:** `CardApplyUseCaseImpl`
**Repository:** `CardJpaRepository` (Spring Data JPA)

| Step | Type | Description | Domain Service |
|------|------|-------------|----------------|
| 1 | `[DB READ]` | Query `card` WHERE `user_id = X-Member-Id` AND `card_type = cardType` AND `status != 'CANCELLED'`; row found → throw `CARD_ALREADY_EXISTS` (CA00009) | `CardQueryService.findActiveByUserAndType()` |
| 2 | `[DOMAIN]` | Generate 16-digit PAN: select BIN prefix by cardNetwork (VISA: `4`, MC: `51`, JCB: `35`), fill remaining digits with `SecureRandom`, validate with Luhn algorithm | `CardNumberService.generate(cardNetwork)` |
| 3 | `[DOMAIN]` | Generate `pan_masked`: replace first 12 digits with `****-****-****`, keep last 4 | `CardNumberService.mask(pan)` |
| 4 | `[DOMAIN]` | Generate `pan_hash`: `HMAC-SHA256(pan, hmacKey)` | `CardEncryptionService.hash(pan)` |
| 5 | `[DOMAIN]` | Generate CVV: `SecureRandom` 3-digit string | `CardNumberService.generateCvv()` |
| 6 | `[DOMAIN]` | Calculate expiry: `applicationDate + 5 years`, format as `MMYY` for storage, `MM/YY` for response | `CardExpiryService.calculate()` |
| 7 | `[DOMAIN]` | AES-256-GCM encrypt: `pan` → `pan_encrypted`; `cardholderNameEn` → `cardholder_name_en`; `cardholderNameZh` → `cardholder_name_zh`; expiry `MMYY` → `expiry_date` | `CardEncryptionService.encrypt()` |
| 8 | `[DB WRITE]` | Insert `card` record: `card_id` = Snowflake, `user_id` = X-Member-Id, `card_type`, `card_network`, `status = ACTIVE`, `pan_encrypted`, `pan_masked`, `pan_hash`, `cardholder_name_zh`, `cardholder_name_en`, `expiry_date`, `activated_at = now()` | `CardJpaRepository.save()` |
| 9 | `[RETURN]` | Return `CardApplyRs` (cardId, maskedPan, cardType, cardNetwork, expiryDate in `MM/YY`, cardholderNameEn decrypted, cvv, status = `ACTIVE`, activatedAt) — **CVV is passed from Step 5 in JVM heap only, never persisted** | — |

---

## 4. Database

### MySQL

| Table | Operation | Description |
|-------|-----------|-------------|
| `card` | READ | Check for existing active card by `(user_id, card_type)` |
| `card` | WRITE | Insert new card record |

### Redis

> This endpoint performs no Redis operations.

### Table Schema

#### card

| Column | Type | Nullable | Default | Description |
|--------|------|----------|---------|-------------|
| card_id | BIGINT | NO | — | PK, Snowflake ID (application-generated) |
| user_id | BIGINT | NO | — | Member ID; referential integrity enforced at application layer |
| card_type | VARCHAR(20) | NO | — | CLASSIC / OVERSEAS / PREMIUM / INFINITE |
| card_network | VARCHAR(10) | NO | — | VISA / MASTERCARD / JCB |
| status | VARCHAR(20) | NO | — | PENDING / ACTIVE / FROZEN / CANCELLED |
| pan_encrypted | VARCHAR(512) | NO | — | AES-256-GCM encrypted full PAN |
| pan_masked | VARCHAR(20) | NO | — | `****-****-****-1234`; stored as plain text for display |
| pan_hash | VARCHAR(64) | NO | — | HMAC-SHA256 of PAN; used for Phase 7 PAN-based lookup; **Unique Index** |
| cardholder_name_zh | VARCHAR(512) | NO | — | AES-256-GCM encrypted Chinese name |
| cardholder_name_en | VARCHAR(512) | NO | — | AES-256-GCM encrypted English name |
| expiry_date | VARCHAR(512) | NO | — | AES-256-GCM encrypted MMYY |
| activated_at | DATETIME(3) | YES | NULL | Timestamp of first ACTIVE transition; written once, never overwritten |
| cancelled_at | DATETIME(3) | YES | NULL | Timestamp of CANCELLED transition; written once, never overwritten |
| created_at | DATETIME(3) | NO | — | Inherited from BaseTimeEntity |
| updated_at | DATETIME(3) | NO | — | Inherited from BaseTimeEntity |

> **Application-layer constraint:** `(user_id, card_type)` WHERE `status != 'CANCELLED'` — enforced by Step 1 duplicate check, not a DB-level partial unique index.
> **Unique Index:** `pan_hash` — prevents duplicate PAN across all users.

#### card_type_limit (seed data, read-only)

| Column | Type | Nullable | Description |
|--------|------|----------|-------------|
| card_type | VARCHAR(20) | NO | PK: CLASSIC / OVERSEAS / PREMIUM / INFINITE |
| single_txn_limit | BIGINT | YES | Single-transaction limit in cents; NULL = unlimited |
| daily_limit | BIGINT | YES | Daily cumulative limit in cents; NULL = unlimited |
| domestic_reward_rate | DECIMAL(5,4) | NO | Base domestic reward rate (e.g., 0.0100 = 1%) |
| overseas_reward_rate | DECIMAL(5,4) | NO | Base overseas reward rate |
| fx_fee_rate | DECIMAL(5,4) | NO | Cross-border transaction fee rate |
| created_at | DATETIME(3) | NO | — |
| updated_at | DATETIME(3) | NO | — |

---

## 5. Error Codes

| HTTP Status | Error Code | Description | Trigger Condition |
|-------------|------------|-------------|-------------------|
| 409 | CA00009 | CARD_ALREADY_EXISTS | User already has an active (PENDING / ACTIVE / FROZEN) card of the same type |
| 500 | INTERNAL_ERROR | Unexpected system error | Unhandled exception |
